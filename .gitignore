.idea/
Cargo.lock
target/
# Where `dev-server` keeps a throwaway Vault's token and state.
vault-dev/
*.deb
*.json
*.pem
*.crt
*.key
*.p12

# Test fixtures are certificates only; generate.sh deletes the keys it makes.
!src/cert/testdata/*.pem
*.pkg.tar.zst
